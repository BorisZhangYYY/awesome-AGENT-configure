<!--
  本文件在每次 Agent 会话启动时自动注入到上下文中。
  handler.js 监听 agent:bootstrap 事件，读取本文件全部内容并追加到 messages。

  你可以在这里写任何需要 Agent 每次会话都知晓的内容，没有格式限制：

  ## 角色身份
  你是 xxx，负责 xxx。

  ## 项目规则
  - 规则 1
  - 规则 2

  ## 工作流
  1. 步骤一
  2. 步骤二

  ## 状态约定
  状态文件路径：xxx
  字段 schema：xxx

  ## 禁止事项
  - 不要 xxx

  使用 {{占位符}} 标记运行时动态值。
--> 