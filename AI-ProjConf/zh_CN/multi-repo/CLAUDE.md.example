# CLAUDE.md

> 本项目采用的默认语言为：简体中文
> 适用场景：微服务工作区 —— 根目录不做版本控制，子目录为独立 git 仓库

## 项目概述

{{项目一句话描述}}

## Agent 规范

### 语言规范

- {{语言风格要求}}

### 文档规范

- 业务文档（验收报告 / 测试报告 / 功能书 / 白皮书等）统一存放至 `.docs/`
- 开发者自用内容（任务验证 / 开发计划等）存放于 `.project/`
- 目录命名：`YYYYMMDD-主题/`，例：`20260831-HIVE建表语句/`
- {{其他文档要求}}

### 任务跟踪规范

> 待办事项「一处登记、一处展开」：根目录 `TODO.md` 是索引，`.project/` 是正文。

- 接到新的待办事项，在 `.project/` 下建目录 `YYYYMMDD-<主题>/`，正文以主题命名
- 同时在根目录 `TODO.md` 的「未完成」下加一行，格式：`### <事项>，见 [<目录名>](.project/<目录名>)`
- 事项完成后从「未完成」移入「已完成/归档」，前缀 `### ✅`

## 环境索引

| 环境 | 连接方式 | 详情 |
|------|----------|------|
| 测试 | {{...}} | `.project/env/test.md` |
| 预发 | {{...}} | `.project/env/uat.md` |
| 生产 | {{...}} | `.project/env/prd.md` |

## 其他

- {{其他约定}}
